import templateUrl from './dashboard.template.html'
import { DashboardController } from './dashboard.controller'

export const dashboardComponent = {
    templateUrl,
    controller: DashboardController,
};
